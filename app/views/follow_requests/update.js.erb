
var targetClass = ".<%= dom_id(@sender) %>_<%= dom_id(@recipient) %>_follow_unfollow";

$(targetClass).html("<%= j(render partial: 'follow_requests/follow_unfollow', locals: { sender: @sender, recipient: @recipient }) %>");
