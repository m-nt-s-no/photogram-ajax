var old_link = "#<%= dom_id(@follow_request.id) %>_likes"
var new_link = "<%= j(render 'users/show', follow_request: @follow_request.id) %>"

// var new_link = "<%= j(render "follow_requests/follow_unfollow", sender: current_user, recipient: @recipient) %>"

// var new_link_2 = "<%= j(render "follow_requests/form", follow_request: recipient.received_follow_requests.build) %>"
// var new_link_3 = "#<%= dom_id(@follow_request) %>"

$(old_link).replaceWith(new_link)
